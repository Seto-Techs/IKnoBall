{
  "name": "@iknoball/prisma",
  "private": true,
  "scripts": {
    "generate": "prisma generate --config prisma.config.ts",
    "migrate": "prisma migrate deploy --config prisma.config.ts"
  },
  "dependencies": {
    "@iknoball/schema": "workspace:*",
    "@prisma/config": "^7.7.0",
    "dotenv": "^16.4.7",
    "prisma": "^7.7.0"
  }
}
